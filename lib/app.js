
module.exports = {

    views: require('./views'),
    shows : require('./shows'),
    rewrites: require('./rewrites'),
    updates: require('./updates')
};

// Bind event handlers
// require('./events')
