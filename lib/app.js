
module.exports = {

    views: require('./views'),
    lists : require('./lists'),
    shows : require('./shows'),
    rewrites: require('./rewrites'),
    updates: require('./updates')
};

// Bind event handlers
// require('./events')
